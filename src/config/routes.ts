import Auth from '../pages/Auth'
import Landing from '../pages/LandingPage'

export const routes = [
    {
        path: '/',
        component: Landing
    },
    {
        path: '/auth',
        component: Auth
    }
]